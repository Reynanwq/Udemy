/*
nomes.splice(quando começar, quando finalizade).
função começa em...
função termina...

NUMBER.MAX_VALUE: referencia ao MAX só que no javascrpt
*/

let nomes = ['reynan', 'joao', 'pedro'];
console.log(nomes.splice(2, 1)); //remove joao
console.log(nomes);