const player = 'Deft';

function nomeDoPlayer() {
    const player = 'Viper';
    console.log(player);
}

function chamaNomeDoPlayer() {
    nomeDoPlayer()
}

chamaNomeDoPlayer();
/* A funcao chama o nome do viper primeiro, pois
Primeiro a funcao busca o que está dentro dela e depois o que está fora
*/